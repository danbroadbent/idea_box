<h1>My Ideas</h1>

  <% @ideas.each do |idea| %>
  <%= idea.name %> - <%= idea.description %> - <%= idea.category.name %> <%= link_to "Delete Idea", idea_path(idea.id), method: :delete %>
  <% end %>

<%= link_to "Create New Idea", new_idea_path %>
